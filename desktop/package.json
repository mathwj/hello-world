{
  "name": "karaokebox-desktop",
  "version": "1.0.0",
  "description": "Desktop shell for KaraokeBox, so the Music page can load youtube.com for real",
  "main": "main.js",
  "scripts": { "start": "electron ." },
  "devDependencies": { "electron": "^33.0.0" }
}
